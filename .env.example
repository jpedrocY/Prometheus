# Example environment file. Copy to .env for local overrides.
# Do NOT commit .env. Do NOT put real secrets here.
# Production Binance trade-capable keys must not be created during Phase 1.

PROMETHEUS_PROFILE=local
PROMETHEUS_CONFIG_PATH=configs/dev.example.yaml

# BINANCE_API_KEY=REPLACE_ME_IF_EVER_USED
# BINANCE_API_SECRET=REPLACE_ME_IF_EVER_USED
